version: "3.9"

services:
  toerekening:
    build:
      context: .
    restart: unless-stopped
    networks:
      - traefik_net
    # No labels – routing via routes.yml

networks:
  traefik_net:
    external: true
